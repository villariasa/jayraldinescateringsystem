# Developer Log: 2026-09-20 (Entry 03)

## Subject: Versioned APK Naming & Metadata Sync
- **Author**: Medy B. Villarias
- **Target**: `build_tablet_installer.py`, `build.gradle`

### Notes & Decisions
Configured APK distribution to always append the active version tag (e.g., `jayraldines_catering_v2.1.4.apk`). The sync server reads `output-metadata.json` and `build.gradle` to ensure download headers and diagnostic links always match the compiled binary.
