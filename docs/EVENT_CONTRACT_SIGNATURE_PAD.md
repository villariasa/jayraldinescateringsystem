# HTML5 Canvas Digital Signature Capture Specification

## 1. Canvas Dimensions & Smoothness
- Smooth bezier curve smoothing applied to touch coordinates using quadratic curves.
- Exported as transparent 300 DPI Base64 PNG image.

## 2. Contract Embedding
The captured client signature is embedded directly into the generated contract PDF above the client acknowledgment block.
